# 灵犀天机 — CLAUDE.md

## 项目概述

灵犀天机是一款 Android 严肃占卜 App（Flutter/Dart），包含四大功能模块：
- **融合占卜**：梅花易数定势 → 级联映射塔罗
- **梅花易数**：传统梅花独立占卜
- **塔罗占卜**：传统韦特塔罗 3 张牌
- **八字命理**：四柱排盘 + 大运流年

完整设计文档见 `docs/PRD.md`。

## 开发规范

### 模块化开发原则

1. **每个功能模块独立成目录**：`lib/pages/fusion/`、`lib/pages/meihua/`、`lib/pages/tarot/`、`lib/pages/bazi/`
2. **数据模型、算法引擎、UI 页面三层分离**，不互相耦合
3. **每个模块可独立调试**：写完后能在手机上单独运行验证
4. **模块间共享代码**放入 `lib/widgets/`、`lib/data/constants/`、`lib/models/` 公共目录
5. **一个文件只做一件事**：算法引擎只负责计算，页面只负责渲染

### 开发阶段

| Phase | 内容 | 交付物 |
|-------|------|--------|
| 1 | 数据层 + 算法引擎 | 塔罗牌义库、64 卦数据、梅花算法、八字算法、SQLite |
| 2 | 梅花 + 塔罗独立模块 | 两个独立 UI + 算法串联 + 翻牌动画 |
| 3 | 融合占卜 | 梅花→塔罗映射逻辑 + 完整 UI + DeepSeek API |
| 4 | 八字命理 | 输入 UI + 排盘展示 + 大运时间线 |
| 5 | 收尾 | 历史记录、设置页、联调测试 |

## 3-Agent 协作开发模式

本项目采用三 Agent 协作模型，遵循以下分工：

### 主 Agent（当前 AI）
**职责：**
- 掌控开发方向和阶段优先级
- 派发开发任务给开发者 Agent
- 验收开发者 Agent 的代码质量
- 汇总测试 Agent 的 bug 报告并安排修复
- 决策层面的问题（架构变更、API 选择等）

### 开发者 Agent
**职责：**
- 所有代码书写实现
- 代码调试和问题排查
- 遵循本文件中的开发规范和 Karpathy 准则
- 提交前自测功能

### 测试者 Agent
**职责：**
- 在开发者 Agent 完成模块后编写测试用例
- 执行测试，记录 bug 并汇报给主 Agent
- 回归验证修复
- 输出测试覆盖率报告

### 协作流程

```
主Agent 判定 Phase → 拆分模块任务
        │
        ▼
    派发给 开发者Agent
        │
        ▼
    开发者Agent: 书写代码 + 自测
        │
        ▼
    主Agent: Code Review + 合并
        │
        ▼
    派发给 测试者Agent
        │
        ▼
    测试者Agent: 写测试用例 + 执行 + 汇报bug
        │
        ▼
    主Agent: 安排修复 → 开发者Agent修复 → 测试者Agent回归
```

## Karpathy 编码准则

### 先思考再编码
- 编码前明确假设，不确定就问
- 如果有多种实现方案，列出权衡再选
- 如果发现更简单的方案，主动提出
- 不清楚需求时，先澄清再动手

### 保持简单
- 只写解决问题所需的最少代码
- 不为未要求的功能做准备
- 不引入不必要的抽象层
- 200 行能解决的不要写 500 行

### 精确修改
- 只修改任务要求的部分
- 不改动周围代码的格式或风格
- 不重构未出问题的代码
- 自己的改动产生的废代码（未使用的 import/变量）要清理

### 目标驱动
- 每个任务拆分为可验证的子目标
- 先写测试（或明确测试方案），再写实现
- 完成标志不是"代码写完"，而是"测试通过/功能可演示"

## 项目技术栈

| 项 | 选型 |
|----|------|
| 框架 | Flutter (Dart) |
| 目标平台 | Android 8.0+ (API 26+) |
| 状态管理 | provider / riverpod |
| 本地数据库 | sqflite / drift |
| API | DeepSeek（代码完成后配置 Key） |
| 加密存储 | flutter_secure_storage |
| 动画 | flutter_animate |

## 参考数据来源

- 梅花易数 + 八字算法数据：`docs（参考）/reference-data.md`
- 八字/梅花数据模型：`docs（参考）/PRD.md`
- 融合映射规则：`docs/PRD.md §3`
- 塔罗牌义：标准韦特塔罗 78 张（需新建 JSON）

## 关键设计决策（简要）

- **融合策略**：梅花定势 → 级联映射塔罗（非分别独立取数后合并）
- **八卦→塔罗**：火→权杖、土→星币、木→宝剑、水→圣杯
- **体用→基调**：用生体→权杖、比和→圣杯+星币、体克用→宝剑、体生用→星币、用克体→大阿卡纳
- **动爻→聚焦**：1-2→过去位、3-4→现在位、5-6→未来位
- **塔罗牌阵**：3 张（过去/现在/未来），逐张翻牌动画
- **八字真太阳时**：城市选填，不填默认北京时间
- **LLM**：DeepSeek API，联网使用，离线提示"需联网"
- **UI**：Material Design 原生安卓风格
- **塔罗牌义**：标准韦特
