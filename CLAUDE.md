# 灵犀天机 — CLAUDE.md

## 项目概述

灵犀天机是一款 Android 严肃占卜 App（Flutter/Dart），包含四大功能模块：
- **融合占卜**：梅花易数定势 → 级联映射塔罗
- **梅花易数**：传统梅花独立占卜（数字/时间起卦）
- **塔罗占卜**：传统韦特塔罗 3 张牌（逐张翻牌动画）
- **八字命理**：四柱排盘 + 大运流年 + 真太阳时

**状态**：全部 5 个 Phase 开发完成，模拟器全功能测试通过。14 条路由全覆盖。

**爻辞数据**：64卦 × 6条 = 384条《周易》标准爻辞，梅花结果页展示
**塔罗牌义**：78张韦特塔罗，正位+逆位完整含义，翻牌页/解读页展示
**静态分析**：`dart analyze lib/` — 零错误零警告。
**构建**：`flutter build apk --debug` / `release` — 均编译通过。

## 开发规范

### 模块化开发原则
1. 每个功能模块独立目录：`lib/pages/fusion/`、`meihua/`、`tarot/`、`bazi/`
2. 数据模型、算法引擎、UI 页面三层分离
3. 模块间共享代码放入 `lib/widgets/`、`lib/data/constants/`、`lib/models/`
4. 一个文件只做一件事

### 开发阶段（全部完成）

| Phase | 内容 | 状态 |
|-------|------|------|
| 1 | 数据层 + 算法引擎 | ✅ |
| 2 | 梅花 + 塔罗独立模块 | ✅ |
| 3 | 融合占卜 | ✅ |
| 4 | 八字命理 | ✅ |
| 5 | 收尾 + 测试 | ✅ |

## 3-Agent 协作开发模式

本项目全程采用三 Agent 协作模型：

### 主 Agent（当前 AI）
- 掌控开发方向和阶段优先级
- 派发开发任务给开发者 Agent
- 验收代码质量，汇总 bug 报告

### 开发者 Agent
- 所有代码书写实现和调试
- 遵循 Karpathy 准则
- 提交前自测

### 测试者 Agent
- 编写测试用例、执行测试、汇报 bug
- 回归验证修复

### 协作流程

```
主Agent → 拆分任务 → 派发开发者Agent → 代码+自测 → 主Agent Review
       ↓                                                     ↓
  派发测试者Agent ← 回归验证 ← 修复 ← 主Agent安排 ← Bug报告
```

## Karpathy 编码准则

- **先思考再编码**：编码前明确假设，多种方案列出权衡
- **保持简单**：只写解决问题的最小代码，不引入不必要的抽象
- **精确修改**：只修改任务要求的部分，不改动周围代码
- **目标驱动**：每个任务拆分为可验证的子目标

## 项目技术栈

| 项 | 选型 |
|----|------|
| 框架 | Flutter 3.44.0 (Dart 3.12.0) |
| 目标平台 | Android 8.0+ (API 26+) |
| 状态管理 | provider |
| 本地数据库 | sqflite |
| LLM API | DeepSeek（需用户在设置页配置 Key） |
| 加密存储 | flutter_secure_storage |

## 关键设计决策

- **融合策略**：梅花定势 → 级联映射塔罗（方案B）
- **八卦→塔罗**：火→权杖、土→星币、木→宝剑、水→圣杯
- **体用→基调**：用生体→权杖、比和→圣杯+星币、体克用→宝剑、体生用→星币、用克体→大阿卡纳
- **动爻→聚焦**：1-2→过去位、3-4→现在位、5-6→未来位
- **塔罗牌阵**：3 张（过去/现在/未来），逐张翻牌动画
- **八字真太阳时**：城市选填，不填默认北京时间
- **离线**：提示需联网，不降级
- **UI**：Material Design 3 原生安卓风格

## 参考数据来源

- 梅花易数 + 八字算法数据：`docs（参考）/reference-data.md`
- 八字节气数据：`docs（参考）/reference-data.md §3.2`
- 融合映射规则：`docs/PRD.md §3`
- 塔罗牌义：`lib/data/tarot_cards.json`（78张韦特塔罗）

## 模拟器测试结果

- 设备：sdk gphone64 arm64 (Android 14 API 34)
- 梅花易数流程：输入→起卦→结果 ✅
- 塔罗占卜流程：抽牌→翻牌→解读 ✅
- 融合占卜流程：输入→卦象→翻牌→融合解读 ✅
- 八字/历史/设置页面：正常显示 ✅
- 运行异常：0 个
| 6 | Android minSdk 配置 | build.gradle.kts → 26 |
| 7 | 主页卡片溢出 3.5px | padding 16→12 |
| 8 | 设置页连接测试类型不匹配 | `_PlaceholderMeihua` → 直接 HTTP 调用 |

### 功能增强
- **心中所问**：梅花易数、塔罗占卜增加可选问题输入框
- **解惑指引**：填问题才调用 DeepSeek API，不填只展示内置数据
- **爻辞详解**：梅花结果页展示384条《周易》爻辞，动爻高亮
- **牌义全文**：塔罗翻牌页+解读页显示完整 meaningUp/meaningDown

### 自签名发布
- `release.keystore` 生成，`flutter build apk --release` 通过（54.9MB）
- APK 位于项目根目录 `Taluo-Meihua-v1.0.0.apk`

## 模拟器测试结果

- 设备：sdk gphone64 arm64 (Android 14 API 34)
- 梅花易数流程：输入→起卦→结果 ✅
- 塔罗占卜流程：抽牌→翻牌→解读 ✅
- 融合占卜流程：输入→卦象→翻牌→融合解读 ✅
- 八字/历史/设置页面：正常显示 ✅
- 运行异常：0 个
